function yba_basicSequencer(group_class, delay) {
    let group_elements = document.getElementsByClassName(group_class);
    if (group_elements[0].getAttribute("data-seq")) {

        let sorted_elements = group_elements.sort(a, b)
    }
}