function MBanimateIn() {
    setTimeout(showMiniBar, 1200);
    setTimeout(showHomeTask, 1200);
    setTimeout(showMessageTask, 1400);
    setTimeout(showCommentTask, 1600);
    setTimeout(showProfileTask, 1800);
    setTimeout(showVideoTask, 2000);
}